<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ohooc.application">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".bottom.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RecyclerViewActivity"/>

        <activity android:name=".login.OtherLoginAct"/>
        <activity android:name=".login.suggest.LoadFragmentActivity"/>
        <activity android:name=".login.suggest.LoginAct"/>

        <activity android:name=".login2.ActionBarActivity"/>
        <activity android:name=".login2.CustomLayoutActivity"/>
        <activity android:name=".login2.CustomLayoutWithToolBarActivity"/>
        <activity android:name=".login2.RecyclerViewActivity"/>
        <activity android:name=".login2.ScrollViewActivity"/>
        <activity android:name=".login2.ToolBarActivity"/>
        <activity android:name=".login2.TranslucentLayoutActivity"/>


    </application>

</manifest>